let belanja = perseInt(prompt("masukan jumlah belanja"))
switch (true){
    case balanja < 100000:
        document.write(`total belanja = ${belanja}`);
        break;
    case belanja >= 100000:
        document.write(`total belanja = ${belanja}`);
        break
    default.writeInt(`kamu salah masukan nilai`)
}